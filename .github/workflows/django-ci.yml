name: Django CI

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ 3.10, 3.11, 3.12 ]

    steps:
      - name: 🔽 Клонирование репозитория
        uses: actions/checkout@v4

      - name: 🐍 Установка Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: 📦 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🛠️ Применение миграций (если нужно)
        run: |
          python todo_list/manage.py migrate

      - name: ✅ Запуск тестов
        run: |
          python todo_list/manage.py test

      - name: 🔍 Проверка качества кода (flake8)
        run: flake8 . --exclude=__pycache__,migrations

      - name: 🎨 Проверка форматирования (black)
        run: black --check .
